# Obtrusive validation CSS class
jQuery script to extend obtrusive validation (web forms) so that a CSS class is toggled on elements with failing validators.

### Dependencies
-	.net 4.6.2
-	TypeScript 1.8
-	jQuery 1.9.1